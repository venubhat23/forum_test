<div id="meeting_schedule_<%= schedule.id %>_status">
  <% if schedule.meetings_generated_at.blank? %>
    <div class="card mb-4">
      <div class="card-header fw-semibold">Generated Meetings</div>
      <div class="card-body text-center py-5">
        <i class="bi bi-exclamation-triangle text-warning fs-3 mb-3 d-block"></i>
        <p class="text-muted mb-0">Meetings haven't been generated for this schedule yet. Refresh the page — if this persists, contact support.</p>
      </div>
    </div>
  <% else %>
    <div class="alert alert-success d-flex align-items-center gap-2 mb-4" role="alert">
      <i class="bi bi-check-circle-fill fs-5"></i>
      <div><strong><%= pluralize(occurrences.size, "meeting") %></strong> scheduled successfully.</div>
    </div>
    <div class="card mb-4">
      <div class="card-header fw-semibold d-flex justify-content-between align-items-center">
        <span>Generated Meetings</span>
        <span class="badge bg-primary"><%= pluralize(occurrences.size, "meeting") %></span>
      </div>
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead>
            <tr>
              <th>Date</th>
              <th>Status</th>
              <th>Attendance</th>
              <% if schedule.fee_amount.present? %><th>Fee Paid</th><% end %>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% attendance_by_meeting = Meeting.attendance_percentages(occurrences, schedule.chapter) %>
            <% paid_counts_by_meeting = Hash.new(0).merge(FeePayment.where(feeable: occurrences, status: :paid).group(:feeable_id).count) %>
            <% chapter_members_count = schedule.chapter.members.count %>
            <% occurrences.each do |meeting| %>
              <tr>
                <td><%= meeting.scheduled_at.strftime("%d %b %Y, %I:%M %p") %></td>
                <td>
                  <% if meeting.scheduled_at.past? %>
                    <span class="badge bg-secondary">Completed</span>
                  <% else %>
                    <span class="badge badge-status-active">Upcoming</span>
                  <% end %>
                </td>
                <td><%= attendance_by_meeting[meeting.id] %>%</td>
                <% if schedule.fee_amount.present? %>
                  <td><%= paid_counts_by_meeting[meeting.id] %>/<%= chapter_members_count %> paid</td>
                <% end %>
                <td class="text-end">
                  <%= link_to forum_chapter_meeting_path(forum_slug: schedule.chapter.forum.slug, chapter_id: schedule.chapter.id, id: meeting.id), class: "icon-btn icon-btn-view", title: "View" do %>
                    <i class="bi bi-eye"></i>
                  <% end %>
                </td>
              </tr>
            <% end %>
            <% if occurrences.empty? %>
              <tr><td colspan="5" class="text-center text-muted py-4">No meetings generated.</td></tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  <% end %>
</div>
