<% content_for :title, "Edit #{@chapter.name}" %>

<div class="page-header">
  <h1 class="page-title h4">Edit <%= @chapter.name %></h1>
</div>

<div class="row justify-content-center">
  <div class="col-12 col-lg-7">
    <div class="card">
      <div class="card-body p-4">
        <%= form_with model: @chapter, url: forum_chapter_path(forum_slug: @current_forum.slug, id: @chapter.id), method: :patch, local: true do |f| %>
          <div class="mb-4">
            <%= f.label :name, "Chapter Name", class: "form-label" %>
            <%= f.text_field :name, class: "form-control form-control-lg", required: true %>
          </div>
          <div class="mb-4">
            <%= f.label :annual_membership_fee, "Annual Membership Fee", class: "form-label" %>
            <%= f.number_field :annual_membership_fee, step: "0.01", min: "0.01", class: "form-control form-control-lg", placeholder: "Leave blank if not fixed" %>
          </div>
          <div class="mb-4">
            <%= f.label :lifetime_membership_fee, "Lifetime Membership Fee", class: "form-label" %>
            <%= f.number_field :lifetime_membership_fee, step: "0.01", min: "0.01", class: "form-control form-control-lg", placeholder: "Leave blank if not offered" %>
          </div>
          <div class="d-flex flex-wrap gap-2">
            <%= f.submit "Save Changes", class: "btn btn-primary btn-lg" %>
            <%= link_to "Cancel", forum_chapter_path(forum_slug: @current_forum.slug, id: @chapter.id), class: "btn btn-outline-secondary btn-lg" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
