<% content_for :title, "Edit Presentation" %>

<div class="page-header">
  <h1 class="page-title h4">Edit Presentation</h1>
  <p class="page-subtitle mb-0"><%= @chapter.name %></p>
</div>

<div class="row justify-content-center">
  <div class="col-12 col-lg-7">
    <div class="card">
      <div class="card-body p-4">
        <%= render "form", presentation: @presentation, url: forum_chapter_weekly_presentation_path(forum_slug: @current_forum.slug, chapter_id: @chapter.id, id: @presentation.id), method: :patch %>
      </div>
    </div>
  </div>
</div>
